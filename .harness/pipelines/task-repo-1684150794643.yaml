pipeline:
  identifier: Build_task_repo_1684150822711
  name: Build task-repo
  orgIdentifier: default
  projectIdentifier: githubtask
  stages:
    - stage:
        name: Build
        identifier: Build
        type: CI
        spec:
          cloneCodebase: false
          execution:
            steps:
              - step:
                  type: GitClone
                  name: GitClone_1
                  identifier: GitClone_1
                  spec:
                    connectorRef: account.Github_OAuth_1678082073279
                    repoName: shivakumar123441/task-repo
                    build:
                      type: branch
                      spec:
                        branch: main
              - step:
                  type: Run
                  name: Run_1
                  identifier: Run_1
                  spec:
                    shell: Bash
                    command: |
                      #!/bin/bash

                      # Install jq if necessary

                        sudo apt-get install jq -y
                        #sudo apt-get upgrade -y

                      #cd shivakumar123441/task-repo/

                      # Read the JSON file content
                      fileContent=$(cat shivakumar123441/task-repo/json.file)

                      # Check the value of "a" and update if necessary
                      if [[ $(echo "$fileContent" | jq -r '.a') != "1" ]]; then
                        fileContent=$(echo "$fileContent" | jq '.a = "1"')
                      fi

                      # Check the presence of "b" and add if necessary
                      if [[ $(echo "$fileContent" | jq -r 'has("b")') == "false" ]]; then
                        fileContent=$(echo "$fileContent" | jq '.b = "1"')
                      fi

                      # Write the modified content back to the JSON file
                      echo "$fileContent" > shivakumar123441/task-repo/json.file

                      # Stage the modified JSON file
                      cd shivakumar123441/task-repo/
                      git credential reject
                      sudo git config --global user.name "shivakumar123441"
                      sudo git config --global user.email "shivakumar123441@gmail.com"

                      git add .

                      # Commit the changes
                      git commit -m "Update JSON file"

                      # Push the changes back to the repository
                      git push
          platform:
            os: Linux
            arch: Amd64
          runtime:
            type: Cloud
            spec: {}
